env:
  - CODECOV_TOKEN="d9c14660-237a-49e5-88c7-ff01f9e6d631"
  - GO111MODULE=on

language: go

go:
  - 1.11.x
  - tip

before_install:
  - go get -t -v ./...

script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)